// env.js.template — create a copy named `env.js` and fill with your real values before deploying.
// WARNING: Do NOT commit `env.js` with real secrets to your git repository.

// Example:
// window.SUPABASE_URL = 'https://your-project-ref.supabase.co';
// window.SUPABASE_ANON_KEY = 'public-anon-key-xxxxxxxxxxxxxxxxxxxxxxxxxxxx';

// Copy this file to `env.js` and update the values, or inject these values with a CI/deploy step.

window.SUPABASE_URL = 'REPLACE_WITH_SUPABASE_URL';
window.SUPABASE_ANON_KEY = 'REPLACE_WITH_SUPABASE_ANON_KEY';
