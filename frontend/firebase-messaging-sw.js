// firebase-messaging-sw.js
// Firebase Messaging fue reemplazado por Web Push nativo (VAPID) para evitar
// conflictos de scope con el service worker principal (sw.js).
// Este archivo se conserva vacío para que el registro no rompa deploys anteriores.
// Si en el futuro se requiere Firebase Messaging, configurar las claves
// exclusivamente a través de variables de entorno del servidor, nunca inline.
